model:
  name: cb
  hyperparams:
    depth: 6
    n_estimators: 1000
    eval_metric: R2
    task_type: CPU
    random_seed: 0
    learning_rate: 0.3
    l2_leaf_reg: 3
    loss_function: RMSE
    metric_period: 100

training:
  checkpoint_name: cb_model.p

inference:
  checkpoint_name: cb_model.p
